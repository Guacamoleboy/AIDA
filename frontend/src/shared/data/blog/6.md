# Time to implement the design phase from last time.

As I always do I'll go ahead and read what my Teacher wants me to read, study and work on.
Today is about implementing the designs I created the last time. The **user-prompt**, **system-prompt** and the **rubric**. Into a working Java application with at least one endpoint.

It's not written anywhere so **I went to another students Github Repository** to check what the students have been doing in class. Looks like they have been working with **REST** using **Spring Boot** so I'll go ahead and create my LLM-API-IMPL as a Java REST Application.

I got a good final grade for my Backend application (REST) so I simply copy paste my files into my repo as they already have exceptions, server, cache and http errors set up and working.

![xl](/devlog/6/1.png)

This allows me to have **Javalin** straight out the bat within 5min. It also allows me to have a fully working logger, pool, thread, properties reader, dotenv reader and multi developer environments set up within the application.

### Architecture first..

In all my projects I always focus more on **Architecture rather than the code** itself. If the code is working but noone can assume where it's working or how it's working then it has no use.

So I'll start by creating some temp files and folders from my teacher sub-tasks and from what I can assume the end result would be.

In this case I'll assume I'll have a **service** for OpenAI and for Evaluation.

![s](/devlog/6/2.png)

The **OpenAIService** class is for OpenAI specific tasks such as rate limit, security, configs etc.

The **EvaluationService** is for evaluation and score calculations.

So I'll only be needing a **EvaluationController** as we call the **EvaluationService** that should be calling the **OpenAIService**.

I'll then go ahead and add my rubric, system-prompt and user-prompts to my **resources/** folder under my Maven project so I can use them for testing later on.

![s](/devlog/6/3.png)

### Designing again .. ?

Yea. Need to design in order to create a good application. This time I need to design the output and input. The output is the hard one really. The input is just the submission.

The output should be specific and have all requirements.

![xl](/devlog/6/4.png)

My teacher tells me what to do basically so I'll just follow along and do as he says. 

![xl](/devlog/6/5.png)

I made a file that can be found under **docs/** that is my version 1.0.0 of my expected output. It is the final output after evaluation. These fields basically makes up a **DTO class**. But before I do that I need to know something.. camelCase? PascalCase, snake_case, kebab-case ? The tutorial is using camelCase but **Google** and other massive platforms use **snake_case** so I'll go ahead and use that instead of camelCase.

The output has a **set of arrays**. Each array should / could be split into its own little DTO and entity / model.

![xl](/devlog/6/6.png)

This gives us a set of DTOs that we now can work with and implement into the final output.

### Connecting to OpenAI using API Key

I'll go ahead and add my** API Key to my .env file** and push it directly to my Github Repository instead of having it blocked in a **.gitignore** file as it has more use on Github.com than locally. It's a joke.. haha.

I'll add the API Key to my .env file though which is a development environment so it'll be **.env.development** and then use that to connect.

![xl](/devlog/6/7.png)

Great. Now that I have my API Key and my **OpenAIService** set up that connects to the OpenAI service and a server with Javalin attached we're ready to test a few endpoints.

![xl](/devlog/6/9.png)

Using the **.http** file under **resources/http/** I'm able to test my REST connection and see if it works as intended.

![xl](/devlog/6/10.png)

As we can see it works with JSON formatting and it works as OpenAI connection. Great. We are in and ready to continue.

### Scrum Framework

Using the Scrum techniques we've learnt and studied in the other Course **(Systemudvikling)** I've been focusing on working the **MVP** way for this specific project. Meaning I always have a working system. I set up Javalin and make sure it works. I test. I make sure that connections work and so on. Always got a product that is working and viable rather than a program that might never run once.

### Rubric and prompt additions

![xl](/devlog/6/11.png)

Finishing the day I can clearly tell that I might've been focusing too much on architecture rather than implementation as I am yet to be able to finish with the implementation.

I'll go ahead and let Codex do the remaining work as **I don't want to spend 16 hours** on something that will never be judged once.
I've already set up the entire base architecture and all files so Codex will basically only need to work for a few minutes to complete the remaining tasks.

![xl](/devlog/6/12.png)

We have our Rubric added and we have our prompts added. Now we just need to implement the scores and make sure it works as intended. I found that the issue was it using an outdated API Docs for the **{{VARIABLE}}** causing it to not work as we can see under here.

![xl](/devlog/6/13.png)

After getting that fixed up **(Using Codex to code and me as the master architect)** I'll test it once again to make sure it works as planned. I didn't get Codex to think. I did the thinking. I knew exactly what was wrong and it simply implemented it for me quickly to save time.

![xl](/devlog/6/14.png)

As we can se the criterion scores are added from my rubric but the **overall_score** isn't present. Why? It's something I chose from the start as Architecture.

I don't want the LLM to do the **overall_score** as the LLM can make mistakes and have issues from time to time. So I'll use the **criterion.score** and sum it up in the backend itself and present it to the JSON output.

![xl](/devlog/6/15.png)

I then make sure every requirement is met and this confirms that we're set.

![xl](/devlog/6/16.png)

### Was it worth it using Codex rather than coding it by hand?

I'd say so. Given the total tokens used for the implementation being somewhere around **$0.1 to $0.6** it is something that wouldve saved both me and my future employer money.

![xl](/devlog/6/17.png)

![xl](/devlog/6/18.png)

It was a rather good coding session and I feel like I finished the task as intended. 

### Future improvements

I'm sure we all can tell that the REST Application isn't complete and is only working at a bare minimum. So it needs everything that a normal REST Application would need in the future. It doesn't even connect to a DB yet or store IDs or Hash to prevent double evaluation and so on.
It's not something I'm going to spend time on, but it's something I'm aware of.

In all my previous projects I've worked hard with Cache and how to prevent data from being loaded multiple times for no reason as it cost money and something our future employees will want to expect from us.

Either way I'm going ahead and Pull Request my feature branch to the main branch as it's ready. It's using my issue template that I have implemented to learn more about Github and how you can customize basically anything.

![xl](/devlog/6/20.png)

![xl](/devlog/6/19.png)

### Final thoughts

I know I didn't get to implement it fully on my own, but I did all the hard work and Codex basically connected the remaining to save me time. After all that's what AIDA is about I'm assuming. Learning how to implement AI into our development environement and programs for efficiancy. In this case it was implement for me and me only.

I checked off my Github Projects to confirm all tasks are done and that the session is concluded.

![xl](/devlog/6/21.png)

**Bye.**